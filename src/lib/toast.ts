export const toast = (message: string, _opts?: any) => {
  // Minimal stub: logs to console. Replace with react-toastify by installing it and reverting imports.
  console.info("[toast]", message);
};
